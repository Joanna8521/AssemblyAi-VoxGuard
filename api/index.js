/**
 * The Vercel entry point. Same handler as the local server, so there is nothing
 * here that can behave differently from what was tested on a laptop.
 */
export { handler as default } from '../server/app.js';
